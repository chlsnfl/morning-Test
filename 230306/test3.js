let one = parseInt(prompt("숫자를 입력하세요"));
let two = parseInt(prompt("숫자를 입력하세요"));

function select (one, two){
    if(one===two){
        document.write(one+two);{
        }
    }else{
        document.write(Math.sum(one+two)*3);
    }
}